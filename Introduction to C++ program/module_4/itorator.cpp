#include <bits/stdc++.h>
using namespace std;

int main() {

    string str = "Hello i'm jayed";
    for (auto it = str.begin(); it < str.end(); it++) {
        cout << *it << endl;
    }
    return 0;
}